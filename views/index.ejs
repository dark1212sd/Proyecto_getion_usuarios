<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Evaluación Node + Mongo - Gestión de Usuarios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f4f7f6; }
        .card { border: none; border-radius: 12px; }
        .table-dark { background-color: #2c3e50; border: none; }
        .btn-success { background-color: #27ae60; border: none; }
        .btn-warning { color: white; }
    </style>
</head>
<body>
<div class="container py-5">
    <h1 class="text-center mb-4 fw-bold text-dark">Gestión de Usuarios (Proyecto Evaluativo)</h1>

    <div class="card mb-5 shadow-sm">
        <div class="card-body p-4">
            <h5 class="card-title mb-3">Agregar Nuevo Registro</h5>
            <form action="/add" method="POST" class="row g-3">
                <div class="col-md-3">
                    <label class="form-label">Nombre</label>
                    <input type="text" name="name" class="form-control" placeholder="Ej: Emerson" minlength="3" maxlength="50" required>
                </div>

                <div class="col-md-3">
                    <label class="form-label">Email</label>
                    <input type="email" name="email" class="form-control" placeholder="usuario@correo.com" required>
                </div>

                <div class="col-md-3">
                    <label class="form-label">Teléfono</label>
                    <input type="text" name="phone" class="form-control"
                           placeholder="04141234567"
                           pattern="^(0414|0424|0412|0416|0426|0212)\d{7}$"
                           title="Debe empezar por 0414, 0424, 0412, etc. y tener 11 dígitos en total" required>
                </div>

                <div class="col-md-3 d-flex align-items-end">
                    <button type="submit" class="btn btn-success w-100 fw-bold">Guardar en DB</button>
                </div>
            </form>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="table-dark">
                <tr>
                    <th class="ps-4">Nombre</th>
                    <th>Email</th>
                    <th>Teléfono</th>
                    <th class="text-center">Acciones</th>
                </tr>
                </thead>
                <tbody>
                <% if (data.length > 0) { %>
                    <% data.forEach(item => { %>
                        <tr class="align-middle">
                            <td class="ps-4 font-weight-bold"><%= item.name %></td>
                            <td><%= item.email %></td>
                            <td><%= item.phone %></td>
                            <td class="text-center">
                                <a href="/edit/<%= item._id %>" class="btn btn-warning btn-sm px-3 shadow-sm">Editar</a>

                                <a href="/delete/<%= item._id %>" class="btn btn-danger btn-sm px-3 shadow-sm ms-1"
                                   onclick="return confirm('¿Está seguro de eliminar a <%= item.name %>?')">Eliminar</a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="4" class="text-center py-5 text-muted">
                            <p class="mb-0">No hay registros disponibles. ¡Agregue el primero!</p>
                        </td>
                    </tr>
                <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>